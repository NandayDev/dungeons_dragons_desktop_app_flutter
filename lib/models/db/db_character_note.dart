class DbCharacterNote {
  DbCharacterNote(this.id, this.creationDate, this.characterId, this.content, this.priority);

  final int id;
  final int creationDate;
  final int characterId;
  final String content;
  final int priority;
}
